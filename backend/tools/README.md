# Backend Tools

Small local diagnostics for checking the configured Gemini client and model
availability. These scripts are not part of the API runtime.

Run them from the `backend/` directory with a local `.env` containing
`GEMINI_API_KEY`.

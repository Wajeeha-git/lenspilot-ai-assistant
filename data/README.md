# Data

Knowledge base files, seed data, and reference datasets for LensPilot AI Assistant.

> **Note:** Large data files (`.csv`, `.json`, `.sqlite`, `.db`) are excluded from version control via `.gitignore`. Only documentation and small reference files should be committed here.
